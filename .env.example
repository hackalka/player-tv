# ===== Tv Player (modelo 100% cliente) =====
# El servidor SOLO sirve archivos estáticos. Apenas necesita configuración.

# Puerto (Railway lo inyecta solo; en local usa 3000)
PORT=3000

# La configuración de Telegram (apiId, apiHash, grupo, etiqueta) está en:
#   public/config.js   (se ejecuta en el navegador de cada usuario)

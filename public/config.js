/* Configuración de cliente (se ejecuta en el navegador del usuario) */
window.CONFIG = {
    // Credenciales de la API de Telegram (https://my.telegram.org)
    apiId: 8952741,
    apiHash: '693fb2da124662dad85b2b337c53a386',

    // Grupo/foro de origen (formato -100xxxxxxxxxx o @usuario si es público)
    groupId: -1003749684388,

    // Marca
    appName: 'Tv Player',

    // Etiquetas: solo se muestran los temas cuyo título contenga alguna de estas
    autoTags: ['playertv:auto', 'tvplayer:auto'],

    // Mensajes a traer por tema
    messagesPerTopic: 80
};
